# ORB + Gamma Exposure Trading Engine -- runtime dependencies
#
# Kept deliberately small. Anything added here must be imported by something
# under src/trading_engine/; see docs/ARCHITECTURE.md.

# Interactive Brokers API (the maintained successor to ib_insync)
ib_async==2.1.0

# Required to run ib_async inside an already-running event loop
nest_asyncio==1.6.0

# Config parsing and validation
PyYAML==6.0.1
pydantic==2.10.6

# NOTE: do NOT add `asyncio` here. The PyPI package of that name is an
# abandoned 3.4-era backport that shadows the stdlib module on modern Python.
#
# Timezone handling uses stdlib zoneinfo -- pytz is not needed.
# pandas/numpy/scipy were only ever used by the removed 0DTE scanner.

# Testing. pytest-asyncio is required by asyncio_mode="auto" in pyproject.toml.
pytest==8.3.4
pytest-asyncio==0.25.2
pytest-mock==3.14.0

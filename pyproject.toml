[project]
name = "trading-engine"
version = "0.1.0"
description = "Local ORB + Gamma Exposure trading engine for Interactive Brokers"
requires-python = ">=3.13"
dependencies = [
    "ib_async==2.1.0",
    "nest_asyncio==1.6.0",
    "pandas==2.2.3",
    "numpy==2.3.4",
    "PyYAML==6.0.1",
    "pydantic==2.10.6",
    "python-json-logger==2.0.7",
    "scipy==1.15.0",
    "pytz==2024.1",
]

[project.optional-dependencies]
dev = [
    "pytest==8.3.4",
    "pytest-mock==3.14.0",
    "pytest-asyncio==0.25.2",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.setuptools.packages.find]
where = ["src"]
